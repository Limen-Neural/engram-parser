# engram-parser

**Main role:** Become an percise brain surgeon for Mixture-of Experts (MoE) layers, and extract the raw weights.  This is gonna I am going learn much more about MoE architecture and how to extract the weights from it.

**Engram Conversion from Static to Voltage:** All about F16-FP32 casting logic belongs and quantization logic.   Uses the static weight matrices and converts them into the exact "engram"structure-the raw voltage vectors-that the SNN requires.